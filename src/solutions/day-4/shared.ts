// import * as fs from 'fs/promises'

// export const fileToPairs = (path: string): Promise<string[][]> =>
//   fs.readFile(path, { encoding: 'utf8' })
//     .then(data => data.split('\n').map(line => line.split(',')))
